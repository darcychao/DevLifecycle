# Public Method Catalog

## Document Metadata
- **Language:** {lang}
- **Total Methods:** {count}
- **Generated:** {YYYY-MM-DD HH:MM}
- **Generated By:** Project Scanner Agent (Phase 0)

---

## 1. Method-to-Module Index

*All exported public methods across the project. Use this index to find whether a functionally equivalent method already exists before creating a new one.*

| Method | Signature | Classification | Provider Module |
|--------|-----------|----------------|-----------------|
| `{name}` | `{full signature with params and return type}` | {classification} | MOD-XXX ({Name}) |

**Classification codes:**
| Code | Description | Verb Pattern |
|------|-------------|-------------|
| `API` | API Communication | `fetch*`, `send*`, `request*`, `upload*`, `api*` |
| `CRUD` | CRUD Operations | `create*`, `update*`, `delete*`, `save*`, `insert*` |
| `VAL` | Validation | `validate*`, `sanitize*`, `check*`, `assert*` |
| `XFMR` | Data Transformation | `format*`, `parse*`, `transform*`, `convert*` |
| `LIFE` | Lifecycle Management | `init*`, `setup*`, `bootstrap*`, `start*`, `stop*` |
| `EVENT` | Event Handling | `subscribe*`, `emit*`, `dispatch*`, `notify*`, `handle*` |
| `COMP` | Computation | `calculate*`, `compute*`, `aggregate*`, `sum*` |
| `QUERY` | Data Retrieval | `get*`, `find*`, `query*`, `list*`, `search*` |
| `UI` | UI Rendering | `render*`, `display*`, `show*`, `draw*`, `mount*` |
| `OTHER` | Other | All other verb patterns |

---

## 2. Module-to-Methods Cross-Reference

*Reverse index: for each module, all its public methods and their consumers.*

### MOD-XXX: {Name}
| Method | Signature | Classification | Consumer Modules |
|--------|-----------|----------------|------------------|
| `{name}` | `{signature}` | {classification} | MOD-YYY, MOD-ZZZ |

*Consumer Modules: lists module IDs that import this method. `—` = no external consumers (module-private or unused).*

---

## 3. Classification Summary

| Classification | Count | Methods |
|----------------|-------|---------|
| API Communication | {n} | `fetchUsers`, `sendRequest`, ... |
| CRUD Operations | {n} | `createUser`, `updateUser`, `deleteUser`, ... |
| Validation | {n} | `validateEmail`, `sanitizeInput`, ... |
| Data Transformation | {n} | `formatDate`, `parseJSON`, ... |
| Lifecycle Management | {n} | `initApp`, `setupDb`, ... |
| Event Handling | {n} | `handleClick`, `emitEvent`, ... |
| Computation | {n} | `calculateTotal`, `sumAmounts`, ... |
| Data Retrieval | {n} | `getUserById`, `listOrders`, ... |
| UI Rendering | {n} | `renderTable`, `showModal`, ... |
| Other | {n} | ... |

---

## 4. Unused Exports (Dead Code Candidates)

*Methods exported but never imported by any other file. Review for removal.*

| Method | File | Module |
|--------|------|--------|
| `{name}` | `src/path/file.ts:{line}` | MOD-XXX |

---

## 5. Cross-Reference Validation

- [ ] Every method in Section 1 appears in its provider module's entry in Section 2
- [ ] All consumer module IDs in Section 2 are valid (exist in `docs/module-map.md`)
- [ ] Every method in Section 4 (Unused Exports) is also listed in Section 1
- [ ] Classification counts in Section 3 sum to total method count
- [ ] Method count matches scanner Phase B.4 inventory count

---

## 6. Appendix

### 6.1 References
- Module Map: `docs/module-map.md`
- Module Details: `docs/modules/MOD-XXX.md`
- Constant Catalog: `docs/constant-catalog.md`
- Terminology Glossary: `docs/terminology-glossary.md`
